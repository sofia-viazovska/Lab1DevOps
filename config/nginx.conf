server {
    listen 80;
    server_name localhost;

    location / {
        proxy_pass http://127.0.0.1:3000; # Reverse proxy to the application
        proxy_set_header Host $host;
        access_log /var/log/nginx/mywebapp_access.log; # Logging
    }
}